<a href="{{ $url }}" class="inline-flex h-10 w-10 items-center justify-center rounded-full border border-white/10 text-slate-300 transition hover:border-sky-400/60 hover:text-white">
    <span class="sr-only">{{ $name }}</span>
    {{ $slot }}
</a>
